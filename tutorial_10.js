let person={
	name:"jonna",
	age:22,
	county:"australia"

}
function logdata()
{
	let linedisplay=person.name+" is "+person.age+" years old and lives in "+person.county
	console.log(linedisplay)
}
logdata()